{
  "name": "root",
  "private": true,
  "version": "1.0.1",
  "description": "",
  "scripts": {
    "dev:server": "pnpm -F @lmh/server run dev",
    "dev:client": "pnpm -F @lmh/client run dev",
    "build": "node build-dist.mjs",
    "build:bash": "node build-dist.mjs",
    "lint": "pnpm -F @lmh/client run lint",
    "typecheck": "pnpm -F @lmh/server run typecheck && pnpm -F @lmh/types run typecheck && pnpm -F @lmh/client run check",
    "test": "vitest"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devEngines": {
    "packageManager": {
      "name": "pnpm",
      "version": "^11.12.0",
      "onFail": "download"
    }
  },
  "type": "module",
  "devDependencies": {
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  }
}
